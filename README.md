# CLI-Based-To-Do-Trackers-Save-Load-Functionality
A CLI-based To-Do Tracker that lets users add, view, edit, and delete tasks directly from the terminal. It supports task prioritization, due dates, and completion status. Tasks are saved to a local file, allowing users to load their data anytime, ensuring simple, fast, and persistent task management.
